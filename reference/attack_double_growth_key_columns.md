# Key columns for attack rate, doubling time and growth rate parameters in the `$params` table from [`load_epidata()`](https://mrc-ide.github.io/epireview/reference/load_epidata.md)

Key columns for attack rate, doubling time and growth rate parameters in
the `$params` table from
[`load_epidata()`](https://mrc-ide.github.io/epireview/reference/load_epidata.md)

## Usage

``` r
attack_double_growth_key_columns()
```

## Value

A `character` vector.
