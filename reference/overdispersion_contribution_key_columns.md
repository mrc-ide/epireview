# Key columns for overdispersion and relative contribution to transmission from human-human transmission parameters in the `$params` table from [`load_epidata()`](https://mrc-ide.github.io/epireview/reference/load_epidata.md)

Key columns for overdispersion and relative contribution to transmission
from human-human transmission parameters in the `$params` table from
[`load_epidata()`](https://mrc-ide.github.io/epireview/reference/load_epidata.md)

## Usage

``` r
overdispersion_contribution_key_columns()
```

## Value

A `character` vector.
