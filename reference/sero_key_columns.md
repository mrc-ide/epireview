# Key columns for serological parameters in the `$params` table from [`load_epidata()`](https://mrc-ide.github.io/epireview/reference/load_epidata.md)

Key columns for serological parameters in the `$params` table from
[`load_epidata()`](https://mrc-ide.github.io/epireview/reference/load_epidata.md)

## Usage

``` r
sero_key_columns()
```

## Value

A `character` vector.
