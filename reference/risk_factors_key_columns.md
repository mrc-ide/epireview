# Key columns for risk factor parameters in the `$params` table from [`load_epidata()`](https://mrc-ide.github.io/epireview/reference/load_epidata.md)

Key columns for risk factor parameters in the `$params` table from
[`load_epidata()`](https://mrc-ide.github.io/epireview/reference/load_epidata.md)

## Usage

``` r
risk_factors_key_columns()
```

## Value

A `character` vector.
